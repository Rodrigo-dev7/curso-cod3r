function repetir(item, quantidade) {
  return Array(quantidade).fill(item)

  // let resultado = []
  // for (let i = 0; i < quantidade; i++) {
  //   resultado.push(item)
  // }
  // return resultado
}
console.log(repetir('Rodrigo', 10));